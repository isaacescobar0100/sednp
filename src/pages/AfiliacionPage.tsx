import React, { useMemo, useState } from 'react'
import { ChevronLeftIcon, ChevronRightIcon, PlusIcon, SearchIcon, XIcon } from 'lucide-react'
import { affiliates } from '../data/mockData'
import { SectionTitle } from '../components/SectionTitle'
import { StatusBadge } from '../components/StatusBadge'

const toneForStatus: Record<string, 'positive' | 'warning' | 'negative'> = { Activo: 'positive', Suspendido: 'warning', Retirado: 'negative' }

export function AfiliacionPage() {
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState('Todos')
  const [showEnrollment, setShowEnrollment] = useState(false)
  const rows = useMemo(() => affiliates.filter((affiliate) => (status === 'Todos' || affiliate.status === status) && `${affiliate.name} ${affiliate.id} ${affiliate.role}`.toLowerCase().includes(query.toLowerCase())), [query, status])
  return <div className="mx-auto max-w-[1440px]"><SectionTitle eyebrow="Gestión de personas" title="Afiliación" description="Administra la base de afiliados y sus novedades sindicales." action={<button onClick={() => setShowEnrollment(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-night px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-night/15 transition hover:bg-night-deep"><PlusIcon className="h-4 w-4"/>Nuevo afiliado</button>}/><div className="rounded-2xl border border-ink/[0.08] bg-white"><div className="flex flex-col gap-3 border-b border-ink/[0.07] p-4 md:flex-row md:items-center md:justify-between"><label className="relative max-w-md flex-1"><SearchIcon className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-ink/40"/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Buscar por nombre, documento o cargo" className="w-full rounded-xl border border-ink/10 bg-canvas/45 py-2.5 pl-9 pr-3 text-sm outline-none focus:border-night focus:ring-4 focus:ring-night/10"/></label><div className="flex gap-2">{['Todos','Activo','Suspendido','Retirado'].map((item)=><button key={item} onClick={()=>setStatus(item)} className={`rounded-lg px-3 py-2 text-xs font-semibold transition ${status===item?'bg-night text-white':'bg-canvas text-ink/60 hover:bg-ink/5'}`}>{item}</button>)}</div></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead className="bg-canvas/65 text-[10px] uppercase tracking-[0.12em] text-ink/45"><tr><th className="px-5 py-3 font-semibold">Afiliado</th><th className="px-5 py-3 font-semibold">Documento</th><th className="px-5 py-3 font-semibold">Cargo</th><th className="px-5 py-3 font-semibold">Vinculación</th><th className="px-5 py-3 font-semibold">Estado</th></tr></thead><tbody className="divide-y divide-ink/[0.07]">{rows.map((item)=><tr key={item.id} className="transition hover:bg-canvas/50"><td className="px-5 py-4"><p className="text-sm font-semibold text-ink">{item.name}</p></td><td className="px-5 py-4 text-sm text-ink/60">{item.id}</td><td className="px-5 py-4 text-sm text-ink/60">{item.role}</td><td className="px-5 py-4 text-sm text-ink/60">{item.type}</td><td className="px-5 py-4"><StatusBadge tone={toneForStatus[item.status]}>{item.status}</StatusBadge></td></tr>)}{rows.length===0?<tr><td colSpan={5} className="px-5 py-12 text-center text-sm text-ink/50">No se encontraron afiliados para estos filtros.</td></tr>:null}</tbody></table></div><div className="flex items-center justify-between border-t border-ink/[0.07] px-5 py-3 text-xs text-ink/50"><span>Mostrando {rows.length} de 312 afiliados</span><div className="flex gap-1"><button className="rounded-md p-1.5 hover:bg-canvas"><ChevronLeftIcon className="h-4 w-4"/></button><button className="rounded-md p-1.5 hover:bg-canvas"><ChevronRightIcon className="h-4 w-4"/></button></div></div></div>{showEnrollment?<EnrollmentModal onClose={()=>setShowEnrollment(false)}/>:null}</div>
}

function EnrollmentModal({onClose}:{onClose:()=>void}) { const [step,setStep]=useState(1); const steps=['Datos personales','Información laboral','Revisión']; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-night/45 p-4"><section role="dialog" aria-modal="true" aria-labelledby="enrollment-title" className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-ink/[0.08] px-6 py-5"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-gold">Afiliación</p><h2 id="enrollment-title" className="mt-1 font-display text-xl font-semibold">Nuevo afiliado</h2></div><button onClick={onClose} className="rounded-lg p-2 text-ink/50 hover:bg-canvas"><XIcon className="h-5 w-5"/></button></div><div className="px-6 pt-6"><div className="grid grid-cols-3 gap-2">{steps.map((item,index)=><div key={item} className="flex items-center gap-2"><div className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full text-xs font-bold ${index+1<=step?'bg-night text-white':'bg-ink/8 text-ink/45'}`}>{index+1}</div><span className={`hidden text-xs font-medium sm:block ${index+1===step?'text-night':'text-ink/45'}`}>{item}</span></div>)}</div><div className="mt-7">{step===1?<PersonalFields/>:step===2?<WorkFields/>:<ReviewFields/>}</div></div><div className="mt-7 flex justify-between border-t border-ink/[0.08] px-6 py-4"><button onClick={()=>step===1?onClose():setStep(step-1)} className="rounded-xl px-4 py-2.5 text-sm font-semibold text-ink/60 hover:bg-canvas">{step===1?'Cancelar':'Anterior'}</button><button onClick={()=>step===3?onClose():setStep(step+1)} className="rounded-xl bg-night px-4 py-2.5 text-sm font-semibold text-white">{step===3?'Confirmar inscripción':'Continuar'}</button></div></section></div> }
function Field({label,placeholder}:{label:string;placeholder:string}) {return <label className="block"><span className="mb-1.5 block text-xs font-semibold text-ink/70">{label}</span><input placeholder={placeholder} className="w-full rounded-xl border border-ink/12 bg-canvas/45 px-3 py-2.5 text-sm outline-none focus:border-night"/></label>}
function PersonalFields(){return <div><h3 className="font-display text-lg font-semibold">Datos personales</h3><p className="mt-1 text-sm text-ink/50">Información de identificación y contacto.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Nombres y apellidos" placeholder="Nombre completo"/><Field label="Documento de identidad" placeholder="Número de documento"/><Field label="Correo institucional" placeholder="nombre@dnp.gov.co"/><Field label="Teléfono" placeholder="300 000 0000"/></div></div>}; function WorkFields(){return <div><h3 className="font-display text-lg font-semibold">Información laboral</h3><p className="mt-1 text-sm text-ink/50">Datos de vinculación en el Departamento.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Cargo" placeholder="Profesional especializado"/><Field label="Dependencia" placeholder="Dirección / Subdirección"/><Field label="Tipo de vinculación" placeholder="Carrera administrativa"/><Field label="Fecha de vinculación" placeholder="dd/mm/aaaa"/></div></div>}; function ReviewFields(){return <div><h3 className="font-display text-lg font-semibold">Revisión de inscripción</h3><p className="mt-1 text-sm text-ink/50">Confirma que la información y autorización de tratamiento de datos estén completas.</p><div className="mt-5 rounded-xl border border-gold/30 bg-gold/10 p-4 text-sm text-ink/70">La solicitud quedará en estado <strong>pendiente de aprobación</strong> por la Secretaría General.</div></div>}
