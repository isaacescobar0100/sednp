import React, { useMemo, useState } from 'react'
import { DownloadIcon, FileTextIcon, SearchIcon } from 'lucide-react'
import { SectionTitle } from '../components/SectionTitle'
import { StatusBadge } from '../components/StatusBadge'
import { documents } from '../data/mockData'

export function DocumentalPage(){const [query,setQuery]=useState('');const [filter,setFilter]=useState('Todos');const filtered=useMemo(()=>documents.filter((doc)=>(filter==='Todos'||doc.type===filter)&&doc.title.toLowerCase().includes(query.toLowerCase())),[filter,query]);return <div className="mx-auto max-w-[1440px]"><SectionTitle eyebrow="Memoria institucional" title="Documental" description="Repositorio seguro de actas, resoluciones e informes de la organización."/><section className="rounded-2xl border border-ink/[0.08] bg-white"><div className="flex flex-col gap-3 border-b border-ink/[0.07] p-4 sm:flex-row sm:items-center sm:justify-between"><label className="relative max-w-md flex-1"><SearchIcon className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-ink/40"/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Buscar por título o código" className="w-full rounded-xl border border-ink/10 bg-canvas/45 py-2.5 pl-9 pr-3 text-sm outline-none focus:border-night"/></label><div className="flex gap-2 overflow-x-auto">{['Todos','Acta','Resolución','Informe','Reglamento'].map((item)=><button onClick={()=>setFilter(item)} key={item} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-semibold ${filter===item?'bg-night text-white':'bg-canvas text-ink/60 hover:bg-ink/5'}`}>{item}</button>)}</div></div><div className="grid gap-4 p-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map((document)=><article key={document.code} className="group rounded-xl border border-ink/[0.08] p-4 transition hover:border-gold/50 hover:shadow-md"><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-gold/12"><FileTextIcon className="h-5 w-5 text-[#9a6b20]"/></div><StatusBadge tone="night">{document.type}</StatusBadge></div><h2 className="mt-4 font-display text-sm font-semibold leading-snug text-ink">{document.title}</h2><p className="mt-2 text-xs text-ink/50">{document.code}</p><div className="mt-5 flex items-center justify-between border-t border-ink/[0.07] pt-3 text-xs text-ink/50"><span>{document.date} · {document.pages} pág.</span><button className="rounded-lg p-1.5 text-night hover:bg-canvas" aria-label={`Descargar ${document.title}`}><DownloadIcon className="h-4 w-4"/></button></div></article>)}{filtered.length===0?<div className="col-span-full py-12 text-center text-sm text-ink/50">No hay documentos que coincidan con la búsqueda.</div>:null}</div></section></div>}
