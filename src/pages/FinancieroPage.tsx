import React, { useState } from 'react'
import { ArrowDownLeftIcon, ArrowUpRightIcon, CircleDollarSignIcon, PlusIcon, WalletCardsIcon } from 'lucide-react'
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { MetricCard } from '../components/MetricCard'
import { SectionTitle } from '../components/SectionTitle'
import { StatusBadge } from '../components/StatusBadge'
import { accountingEntries, cashFlow, formatCop } from '../data/mockData'

export function FinancieroPage(){const [showExpense,setShowExpense]=useState(false); return <div className="mx-auto max-w-[1440px]"><SectionTitle eyebrow="Tesorería" title="Financiero" description="Seguimiento a recaudo, ejecución y movimientos contables del periodo." action={<button onClick={()=>setShowExpense(true)} className="inline-flex items-center gap-2 rounded-xl bg-night px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-night/15"><PlusIcon className="h-4 w-4"/>Registrar gasto</button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Recaudo abril" value="$14,2 M" detail="Meta mensual: $14,0 M" icon={CircleDollarSignIcon} tone="gold"/><MetricCard label="Cartera morosa" value="$1,8 M" detail="13 aportes por regularizar" icon={ArrowDownLeftIcon} tone="brick"/><MetricCard label="Ejecución presupuestal" value="68%" detail="Plan anual de $184,0 M" icon={ArrowUpRightIcon} tone="green"/><MetricCard label="Saldo en caja" value="$27,6 M" detail="Disponible al 30 de abril" icon={WalletCardsIcon} tone="night"/></div><section className="mt-6 rounded-2xl border border-ink/[0.08] bg-white p-5"><div><h2 className="font-display text-base font-semibold">Flujo de caja</h2><p className="mt-1 text-xs text-ink/50">Ingresos y egresos mensuales · millones COP</p></div><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={cashFlow} margin={{top:4,right:4,bottom:0,left:-25}}><XAxis dataKey="month" tick={{fontSize:11,fill:'#768094'}} axisLine={false} tickLine={false}/><YAxis tickFormatter={(v)=>`$${v}M`} tick={{fontSize:11,fill:'#768094'}} axisLine={false} tickLine={false}/><Tooltip formatter={(v:number,n:string)=>[`$${v.toFixed(1)} M`,n==='income'?'Ingresos':'Egresos']} contentStyle={{borderRadius:12,border:'1px solid #e8e7e2',fontSize:12}}/><Bar dataKey="income" name="income" fill="#0F1B3D" radius={[5,5,0,0]} barSize={18}/><Bar dataKey="expense" name="expense" fill="#C9973B" radius={[5,5,0,0]} barSize={18}/></BarChart></ResponsiveContainer></div><div className="mt-2 flex gap-5 text-xs text-ink/60"><span className="flex items-center gap-2"><i className="h-2 w-2 rounded-full bg-night"/>Ingresos</span><span className="flex items-center gap-2"><i className="h-2 w-2 rounded-full bg-gold"/>Egresos</span></div></section><section className="mt-6 overflow-hidden rounded-2xl border border-ink/[0.08] bg-white"><div className="border-b border-ink/[0.07] px-5 py-4"><h2 className="font-display text-base font-semibold">Libro contable</h2><p className="mt-1 text-xs text-ink/50">Últimos movimientos registrados</p></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left"><thead className="bg-canvas/65 text-[10px] uppercase tracking-[0.12em] text-ink/45"><tr><th className="px-5 py-3">Fecha</th><th className="px-5 py-3">Concepto</th><th className="px-5 py-3">Categoría</th><th className="px-5 py-3">Tipo</th><th className="px-5 py-3 text-right">Valor</th></tr></thead><tbody className="divide-y divide-ink/[0.07]">{accountingEntries.map((entry)=><tr key={entry.concept}><td className="px-5 py-4 text-sm text-ink/55">{entry.date}</td><td className="px-5 py-4 text-sm font-medium text-ink">{entry.concept}</td><td className="px-5 py-4 text-sm text-ink/55">{entry.category}</td><td className="px-5 py-4"><StatusBadge tone={entry.type==='Ingreso'?'positive':'negative'}>{entry.type}</StatusBadge></td><td className={`px-5 py-4 text-right text-sm font-semibold ${entry.type==='Ingreso'?'text-emerald-700':'text-brick'}`}>{entry.type==='Ingreso'?'+':'-'} {formatCop(entry.amount)}</td></tr>)}</tbody></table></div></section>{showExpense?<ExpenseModal onClose={()=>setShowExpense(false)}/>:null}</div>}
function ExpenseModal({onClose}:{onClose:()=>void}){return <div className="fixed inset-0 z-50 flex items-center justify-center bg-night/45 p-4"><section role="dialog" aria-modal="true" className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><h2 className="font-display text-xl font-semibold">Registrar gasto</h2><p className="mt-1 text-sm text-ink/50">Ingresa los datos del movimiento contable.</p><div className="mt-5 space-y-4"><FormField label="Concepto" placeholder="Descripción del gasto"/><FormField label="Categoría" placeholder="Seleccionar categoría"/><FormField label="Valor" placeholder="$ 0"/></div><div className="mt-6 flex justify-end gap-2"><button onClick={onClose} className="rounded-xl px-4 py-2.5 text-sm font-semibold text-ink/60 hover:bg-canvas">Cancelar</button><button onClick={onClose} className="rounded-xl bg-night px-4 py-2.5 text-sm font-semibold text-white">Registrar gasto</button></div></section></div>};function FormField({label,placeholder}:{label:string;placeholder:string}){return <label className="block text-xs font-semibold text-ink/70">{label}<input placeholder={placeholder} className="mt-1.5 w-full rounded-xl border border-ink/12 bg-canvas/40 px-3 py-2.5 text-sm font-normal outline-none focus:border-night"/></label>}
